{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn api_server:app --bind 0.0.0.0:$PORT --workers 2 --threads 4",
    "healthcheckPath": "/api/landmarks?city=Dehradun",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
